To do: Find a solution to retrieve stored objects from local storage without losing prototype chain (to inherit methods from parent classes)
sol: loop over data recieved from \_getLocalStorage() and create new objects based on the class based on data recieved
lazy sol applied: disable unnessacry parent method click()
